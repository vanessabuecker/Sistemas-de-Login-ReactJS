import React from "react";

import { ContainerPage, TitlePager } from '../../components/Main';

const Page = () => {
    return (
        <ContainerPage>
            < TitlePager>Configurações</TitlePager>
        </ContainerPage>
    );
}

export default Page;